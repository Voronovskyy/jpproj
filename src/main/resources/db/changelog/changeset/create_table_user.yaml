databaseChangeLog:
  - changeSet:
      id: 3
      author: voronovsky
      changes:
      - createTable:
          tableName: users
          columns:
            - column:
                name: login
                type: varchar(128)
                constraints:
                  nullable: false
            - column:
                name: password
                type: varchar(128)
                constraints:
                  nullable: false
            - column:
                name: role
                type: varchar(128)
                constraints:
                  nullable: false
